import{useEffect,useState}from"react";import{AppShell}from"@/components/layout/AppShell";import{Input}from"@/components/ui/input";import{apiRequest}from"@/services/api";
export function ResidentMonthlySummaryView(){const now=new Date(),[month,setMonth]=useState(now.getMonth()+1),[year,setYear]=useState(now.getFullYear()),[data,setData]=useState<any>(),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);apiRequest(`/residente/resumen-mensual?month=${month}&year=${year}`).then(setData).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[month,year]);const cards=data?[["Visitas",data.visitas.total],["Accesos",data.accesos.total],["Reservas",data.reservas.total],["Amenidades",data.reservas.amenidades_utilizadas]]:[];return <AppShell role="residente" title="Resumen mensual" subtitle="Tu actividad real de visitas, accesos y reservas."><div className="grid gap-3 sm:grid-cols-2"><Input type="number" min={1} max={12} value={month} onChange={e=>setMonth(Number(e.target.value))}/><Input type="number" min={2000} max={2100} value={year} onChange={e=>setYear(Number(e.target.value))}/></div>{error?<p className="text-rose-700">{error}</p>:loading?<p>Cargando...</p>:<><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([x,v])=><div className="rounded-xl border bg-white p-5" key={x}><p className="text-sm text-slate-500">{x}</p><p className="text-3xl font-semibold">{v}</p></div>)}</div><div className="rounded-xl border bg-white p-5"><h3 className="font-semibold">Actividad reciente</h3>{data.actividades_recientes.length?data.actividades_recientes.map((a:any,i:number)=><p className="border-b py-2" key={i}>{a.fecha} · {a.tipo} · {a.detalle}</p>):<p className="mt-3 text-slate-500">Sin actividad en este mes.</p>}</div></>}</AppShell>}
