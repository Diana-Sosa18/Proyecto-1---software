# HU17 Disponibilidad general

`GET /reservas/amenidades/disponibilidad-general?fecha=YYYY-MM-DD` compone para todas las amenidades activas el mismo cálculo backend usado por el flujo individual. La vista `/residente/amenidades/disponibilidad-general` permite comparar franjas disponibles/ocupadas y preseleccionar una franja en el flujo existente.
